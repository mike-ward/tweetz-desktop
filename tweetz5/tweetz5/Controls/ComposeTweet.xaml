<UserControl x:Class="tweetz5.Controls.ComposeTweet"
             x:Name="root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:c="clr-namespace:tweetz5.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="200"
             Focusable="True"
             Height="135"
             Width="200">
    <UserControl.Resources>
        <c:LengthToColorConverter x:Key="converter" />
    </UserControl.Resources>
    <StackPanel Style="{StaticResource ComposeTweet}">
        <TextBlock Style="{StaticResource ComposeTweetTitleBlock}">What's Happening</TextBlock>
        <TextBox x:Name="_textBox"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 Style="{StaticResource ComposeTweetTextBox}"
                 KeyUp="OnKeyUp"
                 IsVisibleChanged="OnIsVisibleChanged"/>
        <DockPanel>
            <TextBlock x:Name="_counter"
                       DockPanel.Dock="Left"
                       Style="{StaticResource ComposeCharCounter}"
                       Text="{Binding ElementName=_textBox, Path=Text.Length}"
                       Foreground="{Binding ElementName=_textBox, Path=Text.Length, Converter={StaticResource converter}}" />
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        DockPanel.Dock="Right">
                <Button Style="{StaticResource ComposeTweetButton}">Shorten</Button>
                <Button Style="{StaticResource ComposeTweetButton}">Send</Button>
            </StackPanel>
        </DockPanel>
    </StackPanel>
</UserControl>
