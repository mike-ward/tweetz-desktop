<UserControl x:Class="tweetz5.Controls.SearchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:tweetz5="clr-namespace:tweetz5"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
      <Grid.Resources>
      </Grid.Resources>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="9*"/>
        <ColumnDefinition Width="1*"/>
      </Grid.ColumnDefinitions>
      <TextBox Grid.Column="0"
               x:Name="_searchText"
               Style="{StaticResource SearchControlTextBoxStyle}">
        <TextBox.InputBindings>
          <KeyBinding Command="{x:Static tweetz5:MainWindow.SearchCommand}" 
                      CommandParameter="{Binding ElementName=_searchText, Path=Text}" Key="Enter" />
        </TextBox.InputBindings>
      </TextBox>
      <TextBlock Grid.Column="1" 
                 FontFamily="Segoe UI Symbol" 
                 FontSize="13">
        <Hyperlink Command="{x:Static tweetz5:MainWindow.SearchCommand}"
                   CommandParameter="{Binding ElementName=_searchText, Path=Text}">
          &#160;&#xE11A;
        </Hyperlink> 
      </TextBlock>
    </Grid>
</UserControl>
